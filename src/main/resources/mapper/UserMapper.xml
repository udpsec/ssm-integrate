<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.inofd.ssm.dao.UserDao">

    <resultMap id="BaseResultMap" type="com.inofd.ssm.entity.User">
        <id column="USER_ID" property="userId" jdbcType="INTEGER"/>
        <result column="USER_NAME" property="userName" jdbcType="CHAR"/>
        <result column="USER_PASSWORD" property="userPassword" jdbcType="CHAR"/>
        <result column="USER_EMAIL" property="userEmail" jdbcType="CHAR"/>

    </resultMap>

    <!-- 根据UserId查询用户数据 -->
    <select id="selectUserById" parameterType="int" resultMap="BaseResultMap">
        select * from t_user where USER_ID = #{userId}
    </select>
    <insert id="insertUser" parameterType="com.inofd.ssm.entity.User">
        insert into t_user (userName,userPassword,useEmail)
        value (#{userName},#{userPassword},#{userEmail})
    </insert>
</mapper>